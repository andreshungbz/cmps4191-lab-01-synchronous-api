$kind: http-request
name: update
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/consumers/{{TEST_CONSUMER_UUID_BCC}}"
method: PUT
body:
  type: json
  content: |-
    {
        "name": "Acme Corp",
        "email": "development@acme.com",
        "status": "terminated"
    }
order: 3920281426307055
